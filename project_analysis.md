# Trading Insight Trainer - 프로젝트 분석 보고서

> [!NOTE]
> 본 문서는 `trading_feedback_webapp_app.py` 코드베이스를 바탕으로 현재 진행 중인 프로젝트의 구조, 주요 기능, 그리고 기술 스택을 종합적으로 분석한 문서입니다.

## 1. 프로젝트 개요
Trading Insight Trainer는 트레이딩 입문자와 일반 투자자가 올바른 투자 판단력을 기르고, 감정적 매매(뇌동 매매)를 피할 수 있도록 돕기 위해 고안된 **교육 및 훈련형 웹 애플리케이션 플랫폼**입니다. 일반적인 차트 분석이나 백테스팅 툴과 달리, 사용자가 거래 시점에 어떤 근거로 판단했는지를 기록하고 그에 대한 직설적이고 객관적인 평가(피드백)를 제공하는 데 초점이 맞춰져 있습니다.

## 2. 사용된 기술 스택 분석 (Tech Stack)

이 프로젝트는 파이썬(Python) 생태계의 데이터 분석 및 웹 애플리케이션 라이브러리들을 활용하여 단일 파일(Singular Script) 아키텍처로 빠르고 효율적으로 구축되었습니다.

- **프론트엔드 & 웹 프레임워크**: `Streamlit`
  - HTML, CSS, JavaScript를 직접 작성하지 않고도 파이썬 코드만으로 인터랙티브한 웹 애플리케이션을 즉각적으로 구성했습니다.
  - Sidebar 라우팅, Tabs, Columns, 데이터 입력 폼(Form) 등 Streamlit의 UI 컴포넌트를 적극 활용하여 사용자 친화적인 대시보드를 구축했습니다.
- **데이터 시각화**: `Plotly (plotly.graph_objects, plotly.subplots)`
  - 교육(튜토리얼) 탭에서 캔들스틱 차트, 시장 구조, 거래량 및 보조지표(EMA, RSI, VWAP, Bollinger Bands)를 시각화하는 데 사용되었습니다.
  - 양방향(interactive) 조작이 가능하고 정교한 차트 렌더링이 필요한 금융 데이터 표시에 최적화된 선택입니다.
- **데이터 처리 모델**: `Pandas`
  - JSON으로 저장된 트레이딩 기록을 DataFrame 파이프라인으로 변환하여, 사용자의 "성장 분석" 탭에서 평균 R:R(손익비), 감정매매 점수, 기록 요약 등 통계치를 쉽게 산출하는 데 쓰였습니다.
- **데이터 저장소**: 로컬 파일 시스템 (`FS`)
  - 별도의 외부 DB(MySQL, PostgreSQL 등)를 두지 않고, 파이썬 내장 `json` 모듈과 `pathlib`을 이용하여 진입 기록을 `data/trades.json` 형태의 파일로 영구 보관합니다.
  - 또한, 다중 타임프레임 차트 업로드 이미지는 `data/` 디렉토리에 직접 바이너리로 저장하고 파일 명칭에 timestamp를 부여하는 직관적인 파일 기반 데이터베이스 방식을 취하고 있습니다.

## 3. 핵심 기능 (Core Features)

- **📚 튜토리얼 및 개념 학습**
  - 실제 시장 데이터가 아닌 **하드코딩된 모의 데이터**를 바탕으로, 이상적인 캔들스틱 형태와 시장 구조, 그리고 지표들의 역할을 시각적으로 교육합니다.
- **🧠 메타인지 퀴즈**
  - 이용자가 단순히 문서를 읽는 것에 그치지 않도록, 튜토리얼에서 배운 내용을 객관식 형태로 묻고 답하는 퀴즈를 제공합니다.
- **📈 논리 검증 훈련 (연습하기)**
  - 가장 핵심이 되는 기능으로, 차트를 볼 때 6가지 사고 순서(추세 판단 -> 위치 파악 -> 캔들 확인 -> 거래량 확인 -> 지표 역할 파악 -> 최종 결론)를 강제하여 구조적인 진입을 유도합니다.
  - 사용자가 입력한 트레이딩 시나리오(진입가, TP, SL, 진입근거)와 심리 상태를 수집합니다.
- **📉 매매 기록 복기 및 성장 분석**
  - Pandas를 이용해 누적된 트레이드 데이터로부터 사용자가 흔히 범하는 감정매매 유형(예: "감정매매 가능성 높음" 횟수)을 집계해 대시보드 형태로 제공합니다.

## 4. 내부 아키텍처 및 로직

> [!IMPORTANT]
> 프로젝트의 꽃이라고 할 수 있는 부분은 `emotion_score`와 `build_feedback` 함수입니다.

1. **감정/이성 텍스트 필터링 (`emotion_score`)**:
   - `weak_keywords` ("그냥", "느낌", "몰빵", "급해서" 등)
   - `strong_keywords` ("지지", "저항", "돌파", "RSI" 등)
   - 입력된 진입 근거(Thesis) 텍스트에 포함된 단어를 카운트하고, 이를 R:R 수치, 체크리스트 달성도, 사용자 체감 조급함/복수심 등과 조합하여 벌점을 부과함으로써 최종 **감정 매매 스코어(0~100점)**를 산출합니다.

2. **교육적 팩트폭력 제공 (`build_feedback`)**:
   - 점수에 따라 "규칙매매에 가까움", "혼합형", "감정매매 가능성 높음"으로 분류한 뒤, 사용자에게 아주 단호하고 직설적인 피드백(예: "TP/SL 구조부터 다시 잡아라. 이 상태는 매매 계획이 아니라 추측에 가깝다.")을 출력하도록 구현되어 현장감 있는 훈육을 제공합니다.

## 5. 향후 개선 가능성 제안 (Next Steps)

> [!TIP]
> 현재 단일 유저 혹은 로컬 환경에 최적화된 MVP 형태이며, 다음 단계로 아래를 고려해 볼 수 있습니다.

- **데이터베이스 구축 및 인증**: `trades.json`에서 SQLite 또는 DB 시스템(Supabase/Firebase 등)으로 전환하여 다양한 기기간 동기화 및 다중 사용자(로그인 시스템) 지원.
- **AI(LLM) 기반의 진입 논리 분석**: 키워드 단어 매칭 방식을 넘어, 유저가 적은 시나리오 텍스트 자체를 의미론적으로 파악하게 하여, OpenAI나 Gemini 모델을 연동하여 1:1 맞춤형 피드백을 주게 하면 훨씬 교육 수준이 정교해질 수 있습니다.
- **기록 시각화 고도화**: 대시보드 쪽에서 날짜별 감정매매 점수 추이를 꺾은선 그래프(Plotly Line Chart) 등으로 보여주어, 자신의 심리 안착 과정이 눈에 띄게 시각화되도록 확장.
